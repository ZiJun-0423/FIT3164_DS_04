import React, { useMemo, useState } from 'react';

export default function PredictionCard({ eloSettings, teams }) {
    const {selectedTeam1, setselectedTeam1} = useState(null);
    const {selectedTeam2, setselectedTeam2} = useState(null);

    
}